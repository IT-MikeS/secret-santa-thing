## Setup DB file for local development

First `cd backend` if not already there.

### Mac/Linux
```bash
touch santa.db
```

### Windows
```powershell
New-Item -Path . -Name "santa.db" -ItemType File
```